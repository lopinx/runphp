# 🏗️ RunPHP 架构设计

> 模块职责、关键设计决策、数据流与测试策略

---

## 📋 目录

- [1. 整体架构](#1-整体架构)
- [2. 模块职责](#2-模块职责)
- [3. 关键设计决策](#3-关键设计决策)
- [4. 数据流](#4-数据流)
- [5. 测试策略](#5-测试策略)

---

## 1. 整体架构

```
┌───────────────────────────────────────────────────┐
│                   用户界面层                        │
│  ┌───────────────┐      ┌──────────────────┐      │
│  │  桌面端 UI     │      │   Web 面板 UI    │      │
│  │  (Tauri 2)    │      │   (浏览器访问)    │      │
│  └───────┬───────┘      └────────┬─────────┘      │
│          │ invoke                │ fetch           │
│          ▼                       ▼                 │
│  ┌───────────────┐      ┌──────────────────┐      │
│  │ Tauri 命令     │      │  axum REST API   │      │
│  └───────┬───────┘      └────────┬─────────┘      │
└──────────┼───────────────────────┼────────────────┘
           │                       │
           ▼                       ▼
┌───────────────────────────────────────────────────┐
│              runphp-core 核心库                     │
│  ┌──────────┬───────────┬────────────────────┐    │
│  │ runtime  │  caddy    │     site           │    │
│  │ (下载)   │ (进程)    │    (CRUD)          │    │
│  ├──────────┼───────────┼────────────────────┤    │
│  │  hosts   │   db      │    config          │    │
│  │ (管理)   │ (SQLite)  │   (持久化)         │    │
│  └──────────┴───────────┴────────────────────┘    │
└───────────────────────┬───────────────────────────┘
                        │
                        ▼
┌───────────────────────────────────────────────────┐
│              FrankenPHP 运行时                      │
│       (Caddy + 内置 PHP，子进程管理)                │
└───────────────────────────────────────────────────┘
```

## 2. 模块职责

### 2.1 runphp-core（核心库）

无 UI 依赖的纯业务逻辑库，三端共用。

| 模块 | 职责 |
|---|---|
| `config.rs` | 应用配置加载 / 保存，站点注册表持久化 |
| `runtime.rs` | FrankenPHP 二进制下载（GitHub Releases）、可安装版本列表拉取、本地已有二进制导入（自动解析版本）、多版本管理、Windows zip 解压、可执行位设置 |
| `caddy.rs` | Caddyfile 模板生成、子进程启停、admin API 状态查询、`reload` 热重载、日志读取 |
| `site.rs` | 站点模型（域名 / HTTPS / Worker / ini 覆盖）、SiteRegistry CRUD、域名校验 |
| `hosts.rs` | 系统 hosts 解析、受管区块标记读写、自动备份、权限检测与提权命令生成 |
| `db/sqlite.rs` | rusqlite bundled 引擎，数据库文件管理、表结构浏览、SQL 执行器 |
| `db/remote.rs` | MySQL / MariaDB / PostgreSQL 连接档案管理 + 表浏览 + SQL 执行；MongoDB / Redis / Qdrant 仅连接测试 |
| `detect.rs` | 本地环境检测：扫描 PATH 中的 FrankenPHP 二进制、探测常见数据库服务端口 |
| `fs.rs` | 文件系统浏览：为 UI 目录选择器提供目录列举（Windows 盘符 / Unix 根、子目录列表、上级导航） |
| `system.rs` | sysinfo 收集主机信息：CPU 架构 / 内存 / 硬盘 / 系统版本，供仪表盘底部状态栏 |

### 2.2 runphp-cli（无头二进制）

| 文件 | 职责 |
|---|---|
| `main.rs` | clap 命令行解析与子命令分发（runtime / site / hosts / run / panel / service） |
| `panel.rs` | axum Web 面板（嵌入式前端 dist + REST API + token 鉴权） |

### 2.3 src-tauri（桌面壳）

薄封装层：每个 `#[tauri::command]` 函数调用对应的 core 方法，不含业务逻辑。

### 2.4 src（前端）

| 目录 | 职责 |
|---|---|
| `api/` | 适配层：桌面端走 `@tauri-apps/api` invoke，面板端走 fetch |
| `stores/` | Pinia 状态管理（运行时 / 站点 / hosts 状态缓存） |
| `views/` | 五个页面：仪表盘、站点、数据库、Hosts、设置 |

## 3. 关键设计决策

### 3.1 为什么用子进程而非嵌入式运行 FrankenPHP？

FrankenPHP 是 Go 编译的 Caddy + PHP 单二进制。Rust 无法直接嵌入 Go 库，因此采用子进程管理：

| 操作 | 方式 |
|---|---|
| 启动 | `frankenphp run --config <Caddyfile>` |
| 热重载 | `frankenphp reload --config <Caddyfile>` |
| 停止 | `POST http://127.0.0.1:2019/stop` |
| 状态查询 | `GET http://127.0.0.1:2019/config/` |

### 3.2 为什么前端一份代码两处运行？

桌面端（Tauri WebView）和 Web 面板使用相同的 Vue 构建产物。`src/api/index.ts` 适配层在运行时检测 `window.__TAURI_INTERNALS__` 是否注入，自动切换 `invoke` / `fetch`，无需维护两套前端。

### 3.3 为什么不捆绑 MySQL / MariaDB / PostgreSQL / MongoDB / Redis / Qdrant 服务器？

控制软件体量。用户通常已有数据库实例，RunPHP 只做连接管理（连接档案 + SQL 编辑器 + 连接测试）。SQLite 作为内置引擎满足零依赖需求。MySQL / PostgreSQL 支持完整的表浏览与 SQL 执行；MongoDB 仅做 TCP 端口探测（避免引入重量级官方 crate）；Redis 走原生 RESP 协议；Qdrant 走 HTTP REST API。

### 3.4 hosts 无权限不硬写

Windows 的 `C:\Windows\System32\drivers\etc\hosts` 和 Linux 的 `/etc/hosts` 需要管理员 / root 权限。设计原则是 **「无权限不静默失败」**：

1. 检测写入权限
2. 无权限时生成提权命令（Windows: `Start-Process -Verb RunAs`，Linux: `sudo`）
3. 用户复制命令手动执行

### 3.5 受管区块标记

hosts 文件中以成对注释标记 RunPHP 托管的区块，区块外内容只读不改：

```
# >>> RunPHP 托管开始（勿手动编辑本区块）>>>
127.0.0.1 mysite.test
# <<< RunPHP 托管结束 <<<
```

每次写入前自动备份到 `hosts.runphp.bak.<时间戳>`。

## 4. 数据流

### 4.1 创建站点并启动

```
用户 → 前端表单 → siteAdd() → core::SiteRegistry.add()
  → config.save_sites()          (JSON 持久化)
  → caddy::write_caddyfile()     (生成 Caddyfile)
  → 用户点击启动 → runtimeStart()
  → core::RuntimeManager.resolve()   (查找运行时)
  → core::caddy::start()             (spawn 子进程)
  → FrankenPHP 读取 Caddyfile，监听端口
```

### 4.2 热重载

```
用户修改站点 → siteUpdate() → 重新生成 Caddyfile
  → runtimeReload() → caddy::reload(cfg, binary)
  → frankenphp reload --config <Caddyfile>
  → Caddy 适配 Caddyfile → JSON → admin API /load
  → 新配置生效，不中断连接
```

## 5. 测试策略

| 层 | 策略 |
|---|---|
| **单元测试** | core 库每个模块附带测试（配置往返、站点校验、hosts 解析、SQLite CRUD/多语句/路径穿越防护、运行时路径），共 13 个 |
| **集成验收** | 每个里程碑用真实操作验证（下载运行时 → 建站 → curl phpinfo → 热重载 → 启停） |
| **前端类型检查** | `vue-tsc --noEmit` 保证类型安全 |
